---
description: Notes create only — writer (STE 1x) + frugal-eval (STE 3x); no git/mail
argument-hint: topic=... path=... [single_doc]
---

Run the **notes-ship** skill with **create_only**.

Load: `.grok/skills/notes-ship/SKILL.md`

Force flags: `create_only` (skip git + mail). Still require writer + frugal-eval with simple-english.

**User args:** $ARGUMENTS create_only
